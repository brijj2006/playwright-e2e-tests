// console.log("Helo World");

export function printName(firstName: string, lastName: string): string {
  let fullName = `Full Name is ${firstName} ${lastName}`;
  return fullName;
}

const logFullName = (firstName: string, LastName: string): string => {
  return `Full Name is ${firstName} ${LastName}`;
};

// console.log(printName("Brijendra", "Singh"));
// console.log(logFullName("MS.", "Dhoni"));
